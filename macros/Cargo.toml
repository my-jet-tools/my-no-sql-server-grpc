[package]
name = "my-no-sql-grpc-macros"
version.workspace = true
edition.workspace = true
authors.workspace = true

[lib]
proc-macro = true

[dev-dependencies]
# The generated code names the core, so testing it needs the core.
my-no-sql-grpc-core = { workspace = true }

[dependencies]
proc-macro2 = "*"
quote = "*"
syn = { version = "*", features = ["full", "extra-traits"] }
